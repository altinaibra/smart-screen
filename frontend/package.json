{
  "name": "smart-screen-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prestart": "npm install --no-audit --no-fund --loglevel=error",
    "start": "vite --open",
    "predev": "npm install --no-audit --no-fund --loglevel=error",
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.103.2",
    "i18next": "^26.4.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-i18next": "^17.0.15",
    "react-router-dom": "^6.28.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "~5.6.3",
    "vite": "^6.0.3"
  }
}
