export type SvgIconProps = {
  /** Madhësia në px (ose çdo njësi CSS). Parazgjedhur: 1em = sa teksti rreth saj. */
  size?: number | string;
  /** Ngjyra. Parazgjedhur: currentColor = ngjyra e tekstit (p.sh. e bardhë në menunë anësore). */
  color?: string;
  className?: string;
};
